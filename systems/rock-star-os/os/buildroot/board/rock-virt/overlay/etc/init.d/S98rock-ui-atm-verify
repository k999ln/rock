#!/bin/sh
set -eu
[ "${1:-start}" = start ] || exit 0
case " $(cat /proc/cmdline) " in
  *' rock.ui.atm.verify=1 '*) ;;
  *) exit 0 ;;
esac
/usr/bin/python3 -I -B /usr/libexec/rock-ui-atm-evidence.py &
exit 0
