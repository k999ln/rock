# D4 diagnostic continuation — 2026-09-10

RQ09/12/16/17 | 明確な楽観主義 | UI 停止直後に検証器が `/proc` 消滅と競合する | 既存 pidfd・署名 B・本物の UI health・A/B 判定を再利用 | crash 観測の 1 条件と host case selector のみ修正 | 元 180 秒・12 GiB・全判定を維持 | Linux 33 tests と image/source guard は合格、実 D4 続行は開始時点では実行中。

配布 source/image/package の producer は `9abf78a80d27aa9f847c4051d20e4c552e407276` のままである。この文書の観測器修正は再 build や配布 archive 更新を行わない。D4 の crash だけ、同 source の別 checkout に diagnostic commit `f7c391c7623c40dd79098e724b9a2b28852ddabf` の 4 files を重ねる。review branch の cherry-pick は `d6647e3`。残りの D4 verifiers は元 9ab の host tools を使うため、全 host tools を同一版と表現しない。

元 D4 全体は **FAIL のまま保持**する。09:50:11–10:11:54 UTC の GUI startup で ready 2、absent 4、freeze 4 の各 case が完了した。crash は最初の 2 起動が合格し、第 3 起動の観測器が失敗、第 4 起動は未実施だった。実行 13 起動、合格 12 起動であり、14 起動合格とは数えない。元 [report](evidence/os-d4-probe-20260910/original-ui-startup-report.json) と [crash boot 3 raw serial](evidence/os-d4-probe-20260910/original-crash-boot-3.log) を保存する。

実 UI の pid/start ticks と loop 進行を確認した後、fixture が同じ pidfd に SIGKILL を送信した。pidfd の即時 poll が一度未準備を返し、直後の `/proc/PID/stat` 読取が `FileNotFoundError` になった。production health は UI を拒否し、mark-good を行わず再起動したが、必要な `ROCK_UI_STARTUP_FAULT` が出なかったため元 verifier は正しく失敗した。修正後の crash は、元 pidfd が readable になるまで元 5 秒期限で待つ。proc の不在を成功と解釈しない。freeze の T/t 確認、pid/start ticks 再確認、health 判定と全 180 秒の起動期限は同一である。

host verifier の `--case crash` は case inventory を全 4 件記録し、crash の 4 起動全てと他 mode の `NOT_RUN` を必須とする。部分結果は `PASS_SCOPED`、`full_matrix_verified=false`。引数なしの既存 default は 14 起動のままである。selector の不完全起動・失敗起動・他 case の合格混入を拒否する負例と、元期限・12 GiB 不変の preflight 負例を追加した。

[Linux regression raw log](evidence/os-d4-probe-20260910/linux-regression.log) は 33 tests / 5.555 秒 / skip 0 で合格した。実 Linux pidfd と SIGKILL を使った試験は終了直前の未準備 poll を一度挟み、その後は同じ kernel pidfd の実 readable と `-SIGKILL` を検証する。UI health はこの単体試験では stub なので、実 UI の証明は後続 4 起動の役割である。Mac の最初の広域実行は Linux 専用 9 件が host を拒否し、実 pidfd 試験は skip した。これを Linux 合格へ読み替えない。

[Overlay proof](evidence/os-d4-probe-20260910/overlay-proof.json) は元 1,364 source files と frozen triple が不変、probe が 3 既存ファイル変更と 1 新試験だけであることを全 SHA で照合する。元 production source guard と ARM64 ELF check を実 image へ適用し、`ui_startup_fixture.py` と S96 fixture の配布 rootfs 不在も debugfs で確認した。post-build から `install-target.sh --include-tests` の明示 install 一覧にも両 fixture は存在しない。これらは verifier が使い捨て signed B へだけ注入する。最初の overlay tar の AppleDouble 4 files は inventory guard が拒否し、既知 metadata を除いた別 proof run で合格した。元 candidate はその間も不変である。

元 D4 の全 85 regular files（全 slot/data、署名 fixture、report、serial、frame）を sparse tar.gz に保存した。host で全 member の size/SHA を元 inventory と照合した archive は `c6009f5bbe563b08cd12ccbe23035a43503972e37a2908555de698789d83cbb3`、714,080,271 bytes。保存先は `/Users/kaiya/Documents/Codex/2026-09-10/j-a-r-v-i-s/work/os-acceptance/final-d4-original-1025.tar.gz`。停止と全 SHA 照合後、Linux の大きな 20 image files を退避済みとして除き、13,176,578,048 bytes の空きを確認した。小さな userdata、全 raw log/report/frame は Linux 側にも残す。失敗状態の bytes は archive から復元できる。

10:33:20 UTC に [新 plan](evidence/os-d4-probe-20260910/continuation-plan.json) を実行前保存した。SHA は `5ccd33fc0e85df395f19a3996d58b9b928becf4c9eb289405cf7f6e143127892`。元 ready/absent/freeze の 10 起動は全 raw log hash と元 validator で再読取する。crash 4 は新 probe、A/B 8、fault 13、auth health 3、data ABI 3 は元 9ab で新規実行する。`validate_health/state/boot/capture` と boot 実行関数の元/probe bytes 一致も事前に要求する。[Continuation driver](evidence/os-d4-probe-20260910/run-d4-continuation.py) は新 31 起動と旧 complete case 10 起動の明示対応が全て成立した時だけ、41 起動の範囲に `PASS_SCOPED` を付ける。元全体 FAIL、旧 crash 2 起動、未実施 gate を合格へ拡張しない。

実行場所は `/var/tmp/rock-release-os-20260910/final-9abf78a-d4-continuation`。producer source は `/var/tmp/rock-final-9abf78a`、probe は `/var/tmp/rock-d4-observer-review-1020`、images は `/var/tmp/rock-final-9abf78a-profile`。この文書の保存時点で continuation は実行中、D4 全体・D1/D2/D3・最終利用一周の合格は未判定である。D6 は別 GitHub run `34465689434` の同じ frozen candidate、新規 authority を使う独立実行であり、ローカル D6 の代用合格や旧 run44 との合成は行わない。
