# Same-image Game nonfinancial gates — 2026-09-10

Requirement/principle/inconvenience/reuse/minimal change/measure/evidence: D3 must exercise the actual final runtime without treating an unavailable remote Wallet as zero or financial equality; reuse Store, Remote, registry-denial and Hub fault verifiers; add one explicit Game scope and a frozen complete external authority observation; preserve every nonfinancial assertion and original time/resource limit; 35 Linux and macOS host tests pass. Real guest execution is pending and must use a new image containing the exact helper and guest sources.

The default `local-full` path retains its original simulator assertions. The explicit `--scope game-isolation` path requires the signed Game image, matching source-checked profile, the profile-bound frozen build record, exact observer/guest helper bytes and a stopped matching external authority. It saves `game-scope-plan.json` before starting a guest, including the complete baseline, image/source/config hashes and unchanged execution limits. No authority start/stop operation is sent by the gate. A running authority refuses observation. After successful guest checks, every external schema, typed row, intrinsic rowid, pragma and protected identity JSON must remain identical. This proves stopped authority retention; it does not claim an online financial transaction or authorization test.

| Gate | Invocation | Preserved scope |
| --- | --- | --- |
| Store | `python3 -B os/verify-store.py --artifacts IMAGES --scope game-isolation` | Two boots, real owned TLS download, v1/v2/rollback approvals, isolated actual results, link loss and offline receipt reuse; 300 seconds per boot |
| Remote | `python3 -B os/verify-remote.py --artifacts IMAGES --scope game-isolation` | Two boots, explicit per-input approval, process isolation, lost accepted reply, exact retry, disconnected pending state, reconnection and offline results; 360 seconds per boot |
| Store denials | `python3 -B os/registry/verify_negative.py --artifacts IMAGES --output NEW --scope game-isolation` | One boot and all ten existing precise denial cases, actual ENOSPC and half-response failures, immutable receipts and normal shutdown; 360 seconds |
| Hub faults | `python3 -B os/verify-hub-faults.py --images IMAGES --output PRIVATE_PARENT --scope game-isolation` | Three boots; fixed launcher crash/deadline/recovery, actual UID1000 request, exact worker PID, original 3-second deadline, no Platform restart, unchanged ordered receipts |

Guest reports explicitly record `wallet: NOT_RUN`, with no financial hash, when the remote Wallet is unavailable. `None` is never hashed as a balance. The local path now rejects a missing Wallet object rather than accepting `None == None`. Store and Remote use an embedded test-only helper installed alongside their existing flagged probes; the host checks its actual image bytes and the guest source bytes. Registry and Hub retain their existing distinct derived fixture image and keep the original image hash fixed. These fixtures do not turn GUI, signed-stage0 or online Wallet items into covered tests.

Hub fault retention reads every table from `backend-cache/remote-cache.db`, the exchange journal and both Game connection databases in Game scope; it separately checks that the two local authoritative Wallet databases are absent in the actual stopped userdata filesystem. Cache schema/rows are exact here: no Game page or authority read is performed, so no freshness exception is applied. All three fault boots retain the same declared scope and cache hash. The external authority remains separately covered by the frozen complete snapshot.

Host validation: seven scope/negative cases, twelve original Hub fault evidence cases, six process inventory cases and ten original registry-negative self-tests, zero failures/skips, on actual Debian ARM64 Linux (`/var/tmp/rock-game-gate-tests-20260910/tests.log`) and macOS. A first command selected nonexistent `test_os_hub_faults.py` and ran zero tests; it was corrected to the actual `test_os_hub_fault_evidence.py` and does not count as a pass. Guest run results, source hashes and the final D3 coverage table must be recorded separately.
