# Final same-image acceptance plan — 2026-09-10

Status: **PLAN, NOT FROZEN / NOT RUN**. This defines the required evidence mapping. The final source commit, build archive and image hashes must be filled by the actual freeze records before execution; no intermediate result is inherited as final acceptance.

Requirement/principle/inconvenience/reuse/minimal change/measure/evidence: RQ01–15 and D0–D6 require one reproducible candidate identity, useful native operations and retained state; older local Wallet observers cannot attest the new remote Game configuration; reuse each applicable native verifier with explicitly scoped additions; fix all source/image/profile/plan hashes before operations; require every mapped result and original numerical limits; this document is a plan and carries no guest PASS.

## Required hash links

| Link | Exact evidence |
| --- | --- |
| Source → native regression | Git archive's full 40-character commit; archive SHA-256; complete `scripts/test-native.py` report SHA; no skipped/failed checks; source unchanged |
| Source → base build | Base `rock-build-freeze/2`, complete `source_files_sha256`, native report and build log SHA, actual cache origin, Buildroot/Linux config SHA, Image/rootfs/stage0 bytes |
| Base → Game image | Exact `base-freeze-manifest.json` bytes and inline equality; `profile_derivation.profile_sha256`; signed factory input/output; unchanged other stage0 entries; complete rootfs inventory and exactly declared additions |
| Final image → every gate | Final freeze raw SHA, source commit, Image/rootfs/stage0 SHA, profile raw SHA, signed factory SHA; expected source files and verifier/orchestrator SHA |
| Game image → authority | Canonical sandbox config raw SHA and authority ID equal to the image's fixed public profile; all external typed tables/schema/identity JSON in stopped snapshots |
| Gate plan → result | Immutable pre-execution plan raw/canonical hashes, complete original result/log/screenshots, stopped disks and independent read-only data comparison; failed attempts retain original status |
| Final image → package | Same final source/image/profile/freeze and signed distribution inventory; B's independent fresh Lima installation and restore results; public fixture UUID/ports stay fixed while databases are independent per VM |
| Build → corresponding sources | Deterministic legal-info bundle manifest with source commit/archive SHA, final freeze/config SHA, exact Buildroot tarball and complete source/license/patch inventory; project-license status remains NOT_CLEARED |

The source archive is immutable evidence. The cache accelerates the build but never substitutes for a native regression or guest result. Bit-identical independent builds are a different claim and are not asserted by these records.

## Required gates and concrete entry points

Paths below are relative to `systems/rock-star-os` unless they begin with `scripts/` (repository root). `IMAGES`, `SOURCE`, `CONFIG` and evidence directories must be resolved absolute paths when the plan is frozen.

| Gate | Entry / owner | Required result and scope |
| --- | --- | --- |
| D0 | `scripts/test-native.py --output NEW`; `os/build-os.sh`; `scripts/freeze-native-build.py`; `scripts/freeze-native-profile.py` / A | All current source checks, actual C build/replay, exact native source unchanged and complete source/build/profile records. The actual execution count comes from this source's report; an older fixed count is not reused. |
| D1 | `ROCK_OS_ARTIFACTS=IMAGES python3 -B os/verify-boot.py`; `python3 -B os/verify-system.py --artifacts IMAGES` / A | Four actual boots total: ARM64 kernel/init, dedicated UID, read-only root, protected separate data, RNG, real Core peer/malformed/oversize/timeout/crash/corruption checks, then a guest-initiated reboot and shutdown with distinct boot identities, exact power receipts and clean stopped filesystem. Direct-kernel tests do not replace signed-stage0 D4. |
| D2 Hub | `python3 -B os/desktop/verify-business.py --images IMAGES --output PRIVATE_PARENT --source-commit SOURCE --mode lifecycle --boot-profile game-authority-ab --device-config CONFIG` / A | Two normal boots; original 16 lifecycle operations and five actual jobs, v1.0→v1.1→rollback, disable/reapprove/delete/reinstall and retained saved results. Complete guest database/profile and empty external authority preservation. |
| D2 native Wallet/Game | Root's source-pinned native C QMP/OCR observers / root | Separate fresh userdata with the same image/profile and still-empty authority. Actual registration, authentication, separate Wallet terms, explicit synthetic credit, independent A/B connection and purchase ceremonies, Hub citation result/history, Wallet balance and restart retention. Follow with separate monthly consent/one USD 8.88 charge/exact same-month retry/cancellation and ATM quote/1000-cent hold/cancel/zero dispense. Signed receipts and full stopped authority/guest data must agree with the displayed result. |
| D3 isolation | `python3 -B os/verify-platform.py --artifacts IMAGES --scope game-isolation` / A | Original scoped 47 guest checks plus 45 independent SDK Tool checks: actual UID/peer/DAC/namespace/seccomp, protected Wallet/cache path rejection, CPU/address-space/file limits, fixed worker faults, signature/approval/input/retry, output and receipt boundaries. Guest financial assertions are explicitly NOT_RUN. |
| D3 Store/Remote | `os/verify-store.py --artifacts IMAGES --scope game-isolation`; `os/verify-remote.py --artifacts IMAGES --scope game-isolation` / A | Four boots total, owned real TLS registry/runner, exact per-package and per-input approvals, remote isolation, accepted-reply loss, same-key recovery, no unauthorized fallback, link loss/reconnect and offline result retention. No unavailable Wallet is treated as zero or financial equality. |
| D3 denials/fault recovery | `os/registry/verify_negative.py --artifacts IMAGES --output NEW --scope game-isolation`; `os/verify-hub-faults.py --images IMAGES --output PRIVATE_PARENT --scope game-isolation` / A | Four boots total; all ten existing precise denial cases including real signature failure, half responses and ENOSPC; then launcher crash/deadline/recovery with original process/UID/3-second deadline/receipt conditions. Distinct injected diagnostic rootfs preserves every original runtime file. Each gate also fixes and compares the complete stopped external authority. |
| D3 online financial complement | Root's actual C Wallet/ATM flows plus D's actual managed TLS actor/replay/deadline tests / root,D | Covers the financial assertions excluded from the offline guest scope: registration/identity/auth/terms/monthly/hold/fee/receipt/actor and replay boundaries. The source-pinned table must distinguish guest IPC checks from host TLS checks and any still-unmeasured machine/dispense scenario. |
| D4 | `os/update/verify-ui-startup.py`, `verify-qemu.py`, `verify-faults.py`, `verify-auth-health.py`, `verify-data-abi.py` / A | Fixed sequence 14+8+13+3+3=41 boots, all original limits. Actual draw/loop startup health, nonce/PID/UID/exe/socket, A/B integrity/interruption/space failures, unavailable auth health and data ABI failures. Faults use the immediately preceding A/B evidence. Every original candidate image byte remains fixed. |
| D5 | Root normal restart/stop UI; B `preview` aggregate backup/current-copy restore and fresh-VM/off-VM export / root,B,D | Same final image on independent fresh VM; nonzero consent/Game/Wallet receipts; stopped OS A/B/data plus complete Wallet/entitlement/C/router/Game source; current-copy lineage, pending interruption/recovery, source writer fencing, fresh restored boot and read-only logical comparison. Include configured/used registry/runner state where applicable. |
| D6 | `python3 -B os/desktop/verify-business.py --images IMAGES --output PRIVATE_PARENT --source-commit SOURCE --mode soak --boot-profile game-authority-ab --device-config CONFIG` / A | Five distinct normal boots, 61 scheduled soak jobs over 3600–4200 seconds, complete native UI/result evidence, exact receipts and zero unrequested financial/member/auth/remote effects; all guest DBs and complete external authority preserved. This runs before enrollment or credit. |

## Ordering and preserved thresholds

Run D4 first. Then explicitly prepare the fresh fixed default authority, verify its initial 19 financial/auth/member tables are empty, and freeze the D6 plan. D6 precedes D2 lifecycle, which uses a different fresh userdata disk and leaves the same authority empty. D1 and the nonfinancial D3 gates use their own disks and stopped authority. Before root's native first-use work, create another fresh `rock-final-c-ui` device config using `os/game_exchange/profile.py device-config`; only the device name changes, while image/profile/authority bytes stay identical. No existing Hub jobs from D2/D6 are presented as a first-use result. B's fresh Lima has independent databases and can run installation/restore acceptance against the same public image in parallel.

D6 retains boot 180 s, shutdown 60 s, UI state 30 s, observed job 90 s, stored job 30 s, maximum start gap 180 s, peak RSS 2 GiB, RSS growth 512 MiB, average QEMU CPU 3.5 cores, 2-second sampling with maximum 10-second gap, original screenshot/evidence budgets and OCR minimum confidence 45. Zero tolerance remains in place for missing/duplicate/changed receipts, schema or financial mutation, forced QMP reset/shutdown, filesystem/journal errors, missing UI states and OCR uncertainty. A failed observation is preserved and a new fixed run is required after a correction; criteria are not loosened.

## Explicit exclusions and unresolved gates

The old `ui/verify-native.py`, `ui/verify-power.py`, `os/verify-atm.py` and the full local Wallet phase of `verify-platform.py` assume a local authoritative Wallet and must not be relabeled as Game evidence. `verify-backup.py` disk-only restore deliberately rejects schema7; use B's complete aggregate restore and reuse its closed database comparison helper. D3's host TLS actor/machine-assertion results are not an actual guest ATM-machine dispense run. The precise D3 complement table must keep such distinctions visible rather than asserting the entire old local fixture was run against Game.

Physical BlackBerry/USB, real funds, production signing/authenticators, unrestricted providers, legal redistribution clearance and a general security guarantee remain outside this development acceptance. Any required D0–D6 item still FAIL/NOT_RUN prevents the complete QEMU candidate acceptance claim. The old run44 and all intermediate image successes remain useful separate evidence only.
