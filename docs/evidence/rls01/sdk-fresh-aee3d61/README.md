# Fresh SDK example, internal synthetic acceptance

RQ-16/RQ-17: game owners/authors need a usable integration example and a way to diagnose and recover retained requests. Reuse the Python SDK, explicit public configuration and private client state; preserve the authority's offline cache contract and classify its connectivity flags correctly. No protocol, claim deadlines or financial rules were relaxed.

An independent fresh Lima VM ran source `aee3d6103a0d9a0439f49ad27a7195af56891589`. Its initial authority was empty and stopped, and both owner and author client stores were new. The example completed Game A, stopped the actual TLS authority, retained the first unsent Game B quote as UNKNOWN, diagnosed unavailable connectivity, restarted the same authority and completed the original request. Both history entries committed 103 synthetic cents with hold 0. An independent stopped/fenced observation matched the harness's entire final 5-database/71-table snapshot. Wallet AVAILABLE is 9794 cents, Game fee total 6 cents, hold 0, and each Game has exactly one 10-unit grant; both issuer epochs remain 1.

Machine times after VM provisioning: first exchange 7.196284 s, cause classification 0.239930 s, recovery 2.220017 s, complete harness 11.700720 s. The two inputs are fixed public configuration and private client state; example nonblank/noncomment code counts are owner 110 and author 38 lines. These are internal machine measurements, not a human or independent external author study. The injected failure was a stopped endpoint before send; it is not presented as a response-loss-after-commit experiment.

The earlier fresh `9cfe6e7` run failed because its diagnostic treated a successful stale cache return as verified live TLS. Its original FAIL and event hashes remain in the adjacent evidence. The corrected run used another new VM and state; it did not overwrite or continue the failed baseline. Raw event/client data remains private on the host. Only owned VMs were normally stopped and deleted, without force.
