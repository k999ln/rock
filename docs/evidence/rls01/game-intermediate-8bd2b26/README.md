# Intermediate Game installer lifecycle

RQ-12/RQ-16/RQ-17: a fresh owner installation must preserve completed work and synthetic Game balances across restart and a recoverable whole-system current copy. Reuses signed source/image verification, existing Lima/desktop lifecycle, OS backup and the independent sandbox/C restore API; no product source or QEMU state was modified for injection. The only forced termination was the explicitly planned test transaction process during a new destination disk copy.

Source `8bd2b26b23bbb953c12027f69d0f68297c49e2ae`, package SHA `001a43c10df411496ce3df4604e06ed3c1bcb1d2ac27ebbcbd3fe32c5b5f8d56`. This is intermediate evidence, not final release acceptance.

The same 9 package outputs reproduced byte for byte. A new private Lima VM installed the exact signed Game image and initialized independent, empty fixture authority data. Manual actual framebuffer UI installed citation, processed the bundled sample, explicitly registered Wallet/authenticator/terms, credited public synthetic $100, separately consented to Game A, and approved one 10-unit purchase. Restart and restored boot displayed the same result, Game A completion and $98.97/hold $0. Monthly payment consent and Game B purchase were not performed.

The stopped complete 19-file backup contains OS A/B/data and all 5 authoritative databases plus retained C/router/configuration JSON; all 1,074,564,436 bytes were verified again in the separate host export after deleting only the owned VM. Authority current-copy finished, then a test process received real SIGKILL after writing/fsyncing 16 MiB of the first destination OS disk. Host start, authority start and direct source OS start refused the aggregate pending state. Repeating the exact same intent with the unmodified delivered installer returned the original authority completion receipt, copied all 3 OS disks exactly, completed the aggregate gate, and permanently refused the retired source. Both Game issuer epochs became 2 exactly once; Game A retained its one grant and 10 purchased units. Three boots reached native UI health and A/B confirmation and ended through native power controls.

`original-business-compare.json` keeps the original frozen observer FAIL (`game_connection_a`) caused by SDK read-clock advancement. `supplemental-plan.json` pins A's later `7c5b41a` observer; its separate stopped-disk audit passed with only the declared typed SDK clock columns advancing. This supplemental result does not turn the original release check into a PASS.

A separate observer selected before the restored UI cycle captured all 7 post-copy authority databases/122 tables under every writer fence. Only the index identity's bounded INTEGER `maximum_time` advanced; all other typed cells/schema/sequences/retained identity JSON were unchanged. Both captures used the same `fb959c9` external read-only auditor; comparison additionally applied `483f1cc`'s stricter 53-bit protocol bound. The earlier restart snapshot pair lacked this typed projection, so its raw identity-table difference remains unclassified rather than being retroactively accepted.

No real funds, production keys, physical-device claims, historical rollback, cross-host import, automated QMP/OCR timing claim or final release approval is implied. Removal used no force and preserved the pre-existing port 8899/PID 77657 viewer. The host package, receipts and exported backup remain; exporting does not enable import after VM deletion.
