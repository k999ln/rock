# Original C sequence and remaining-stage boundary

The original sequence passed Game (two normal boots), the stopped independent ledger audit and financial UI (two normal boots). The owned citations runner failed before guest boot because the inherited Lima umask0002 made its generated executable mode0775. The unchanged executor correctly refused the group-writable file. The original orchestration and citations reports remain FAIL; no financial stage was repeated.

Only this failed probe’s registry process was stopped after exact argv/cwd/start ticks and pidfd validation. The next parent cannot recover its exit code, so the stop proof records SIGTERM and observed exit without fabricating a child return code or guest shutdown. Full stopped authority and all eight exported guest database bytes matched the original preflight baseline. The first read-only collector lacked `/usr/sbin` for debugfs after stopping the registry; its partial original evidence remains private. The second collector uses the declared Linux PATH and makes no second process stop.

The separately frozen [v4 plan](../launch/final-c-continuation-plan-v4.json) sets process umask0077 and calls only the unchanged citations/demo probes in a new directory. It pins the failed original, logs, boundary proof and existing configuration and retains all original UI, time, input and ledger checks. The independent B review verified identical command-building and execution/retention AST for those remaining stages. Final completion is recorded separately after the new run; this document is not a PASS for the original sequence.
