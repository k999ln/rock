from pathlib import Path
import json, math, re

ROOT = Path(__file__).resolve().parents[2]
W = ROOT / 'work/autolink'
text = (W / 'design.md').read_text()
agent = (W / 'agent_connection.md').read_text()
state = agent[agent.index('| 状態 |'):].split('\n\n')[0]
text = text.replace('{{CONNECTION_STATES}}', state)
text = text.replace('4KiB×1000件の本文量は', '一意IDの記録と対象処理は同じトランザクション等で一体として確定し、処理後・記録前の電源断による二重実行を防ぐ。外部サービスへの操作は相手側の一意ID受付または結果照会を必要とし、未対応の操作を自動再実行しない。「端末へ保存」「送信」「相手の受領」「処理完了」を分けて表示する。\n\n4KiB×1000件の本文量は')
text = text.replace('料金は設定上限80%', '端末ごとの認証鍵を保護し、失効と更新に対応する。IP経路は認証済み暗号化通信、非IP衛星経路を採用する場合は検証済み方式によるメッセージ保護を用いる。証明書や鍵の検証を無効にして接続を通さない。\n\n料金は設定上限80%')
text = text.replace('単一リンク計算である。', '利用者リンク単独の計算である。透明中継におけるフィーダーリンクの雑音、中継器雑音・歪みを合成したサービス両端の通信余裕ではない。')
text = text.replace('同一ID100回再送でも対象処理は1回、期限切れは拒否', '処理・ID記録の各境界で電源断を注入。同一ID100回再送でも対象処理は1回、期限切れは拒否')
result = json.loads((W / 'coverage_screen_results.json').read_text())
n = result['points'] * result['time_samples']
rows = []
for v in result['results']:
    missing = round((1-v['mean_point_time_visibility_fraction'])*n)
    rows.append(f"| {v['satellites']}機 / {v['planes']}面 | {v['mean_point_time_visibility_fraction']*100:.5f}% | {missing:,} / {n:,} |")
coverage = '''### まず1機で見える範囲を計算する

地球半径6,371km、高度550km、最低仰角25度の球対称モデルでは、衛星直下から可視境界までの地心角は約8.459度、地表距離は約941km、境界での斜距離は約1,123kmとなる。1機の幾何学的な可視面積は全球の約0.5439%。2機でも重なりなしの上限は約1.0877%である。

地心角はψ=acos[R cos(e)/(R+h)]−e、地表の可視半径はRψ、面積比はf=(1−cosψ)/2で求めた。1/fを切り上げると184機になるが、これは面積だけの必要下限であり、184機なら全球を埋められるという意味ではない。軌道、重なり、時間変化、アンテナの指向範囲を考慮する前の下限である。

既存の傾斜角53度では、幾何学的可視域の緯度上限は約±61.46度となり、極域は覆えない。全球を目標にする配置比較では傾斜角87度を仮定した。

### 3案を同じ条件で比較した結果

高度550km、傾斜角87度、1面24機を共通にし、12面・18面・24面を比較した。地表2,048点と24時間中300秒間隔の288時点、計589,824組を評価した。表の「可視」は、1機以上が最低仰角を超えるという幾何条件だけを指す。

| 試した配置 | 地点×時刻の平均可視率 | 衛星が見えない標本数 |
| --- | --- | --- |
''' + '\n'.join(rows) + '''

いずれの試行配置にも見えない地点・時刻があり、全標本での可視という条件を満たさなかった。576機案でも空白が5標本残り、平均値を100%へ丸めて常時接続と扱わない。432機案と576機案は軌道面・位相の再検討候補とするが、採用機数は確定しない。今回の結果は、この配置と位相についての結果であり、同じ機数の最適配置が不可能という証明ではない。

### 計算の再現条件と限界

地表点はFibonacci球面配置：k=0〜2047、z=1−2(k+0.5)/2048、経度=π(3−√5)k。真の極点は含まない。面番号j=0〜P−1の昇交点赤経はπj/P、面内番号s=0〜23の位相はu=nt+2π[s/24+j/(24P)]とした。標準Walker配置の最適解と主張するものではない。

時刻tは0〜86,100秒、刻み300秒。重力定数μ=398,600.4418km³/s²、n=√[μ/(R+h)³]、軌道周期約95.50分、地球自転角速度7.2921159×10⁻⁵rad/s。地球固定座標に変換し、地上点と衛星の単位位置ベクトルの内積がcosψ以上なら可視とした。

この離散計算は標本間の空白を見落とし得る。全標本が合格した場合でも連続全球可視の証明にはならず、平均可視率は通信サービスの稼働率ではない。次の評価は細かい時間・地点、極点、軌道誤差、故障、ビーム、干渉、地形・建物、容量、ゲートウェイ同時可視を含める。必要な地域と待ち時間を満たすことを確認してから、衛星数と補充計画を決める。
'''
text = text.replace('{{COVERAGE_SECTION}}', coverage.strip())
(W / 'assembled.md').write_text(text)
print(json.dumps({'characters': len(text), 'missing_samples': [round((1-v['mean_point_time_visibility_fraction'])*n) for v in result['results']]},ensure_ascii=False))
