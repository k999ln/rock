# Rock OS — Sky・Wallet・Game 設計書

2026-09-12 Sky改称追記: 利用者向けの自動化入口をSkyへ統一した。既存データ/APIの内部 `hub` 名は互換維持のため残す。[Skyの役割・図・収録ツール](sky.md)を現行の名称と価値説明の正本とする。

2026-09-09承認追記: v1.1の実装承認を受領。後続の明示指示により、CM発表に向けた導入可能版の準備も開始する。原設計のhash・提示版と条件付き了承は [承認記録](execution-approval-20260909.md)、追加の配布条件は [導入・リリース計画](release-installation-plan-20260909.md) に分けて保存する。達成演出は見送り。

版: 1.3 / 2026-09-12 / 状態: **実装承認済み・Developer Preview配布準備中**

これは実装を始める前に確認する設計書。既存の確定要望は製品ベースv1.6/RQ01〜RQ15で保持し、今回の画面・体験案は下記「提案」と区別する。本書を保存しただけでは承認・実装・安全試験完了にならない。

## 0. 設計と実装の現在地

2026-09-09 16:51 UTCの3branch照合。詳細なSHA・コード根拠・修正対象は [相違監査](design-implementation-alignment-20260909.md)。以下の後続節は目標設計であり、全て実装済みという意味ではない。

| 部分 | 実際の現在地 | 次に必要なこと |
| --- | --- | --- |
| main | 既存Web/Android P1と旧ベース。native未マージ | nativeと設計review branchを合わせるB04 |
| native OS/Sky | QEMU用OS・Sky・隔離の試作、source回帰成功 | 最新統合sourceから新imageをbuildしD0〜D6検証 |
| Wallet | 単一ownerの認証付き複数端末・模擬台帳・ATM経路 | 一般の複数owner/player分離と本人接続をGX00で設計 |
| backup/更新 | A/B/data保存本体あり。復元試験入口が旧形式、台帳移行engineなし | 試験入口の形式対応、台帳変更と旧OS/復旧の互換試験 |
| ゲーム交換/作者SDK | 検査対象で未実装 | GX00→GX01→DX01。実ゲームGX02は別条件 |
| 実装と配布 | 実装branchとPRに保存済み。QEMU/Mac試用はあるが汎用installerは未完成 | D6/CIを通し、fresh環境用Developer Previewを作る。実機は対象機種確定後 |

main/nativeのCI成功をreview branchのCIと混同しない。監査時のreview SHAにはcheck-runなし。作業件数の比率はOS完成率ではない。

## 1. どんな端末・基盤にするか

Rockは「自動化の準備、稼働状況、成果、お金を一か所で管理できるOS」。ゲーム接続・交換機能は確定要望であり、利用者がGame入口を使うかは任意とする。自作ゲームの作者が共通Walletへ簡単につなげられる基盤を目指す。

目指す価値は、自動化が作る時間・収入の余地を、本人が選ぶ創作・学習・ゲーム・現実の挑戦につなげること。Skyで仕事を任せ、Walletで確定収支を把握し、許可された連携先で使う。単に取引や賭けの回数を増やす目的ではない。自動化の収益やゲーム資産の価格上昇、社会全体への効果は保証しない。

将来のゲーム経済との接続を見据えるが、GTA VI等の未確認仕様に依存しない。ゲーム側が公式に認める接続方法・権利・操作範囲を確認し、残高閲覧/購入/譲渡/換金など対応能力を区別する。価格が付くことと、自由に交換/移転できることは別。特定ゲーム対応は正式接続と試験後のみ表示する。

利用者にとっては、サービスを巡回せず仕事の状態を確認し、確定した収入とゲーム資産を混同せず管理できる。ゲーム作者にとっては、Walletの認証・取引履歴・二重処理防止を自作せず、安全な接続契約から始められる。

「ゲームを入れればPCにはない価値が自動的に生まれる」「市場で一番便利」は現時点で未実証。外出中の操作時間や、作者の導入時間・復旧時間を測って価値を判断する。

## 2. 変えないことと、今回確認すること

| 区分 | 内容 |
| --- | --- |
| 確定 | OSの中心はSky＋Wallet。tob側が自動化商品を開発し、Rockは共通管理基盤を作る |
| 確定 | 既存Git内のツールもSkyの商品として使い、実処理と失敗/再開を検証する |
| 確定 | ゲーム連携とゲーム内通貨交換、作者向け組み込みやすさを作る。ATMとは独立 |
| 確定 | ATMでRockが取る手数料は0。ゲーム料金は未定。既存OS月888 centsは今回変更しない |
| 確定 | まず現行Linux/Buildroot/ARM64 QEMUで稼働可能な開発OSを作る。実機/本番とは別判定 |
| 確定 | Sky/Walletを主に据え、ゲーム接続・交換機能を実装する。Game入口の利用は任意 |
| 見送り | 仕事の完了などを見せる達成演出は実装しない |
| 未決 | ゲーム名・最初のSDK対象、交換方向/レート/原資/上限/返金、正式providerと提供条件 |
| 未決 | Rock端末を持たないプレイヤーの本番Wallet利用資格。作者sandboxとOS購入/月額契約は別 |
| 検討のみ | Polymarket型予測市場とゲーム資産売買。別設計・別承認が必要で、現在の実行範囲に追加しない |

## 3. OSの入口と利用体験

### Sky — 自動化を使う

商品を選ぶ→必要なPC/cloud/端末と契約を確認→許可・費用を確認→実行→結果・失敗・停止/再開を確認する。商品内部の業務処理はtobの商品が担当し、OSは接続と安全な管理を担当する。

Skyの優位性は特定の処理アルゴリズムではなく、実行場所や提供者が違うToolを同じ順序で制御できることに置く。

1. 目的からToolを探す。
2. 作者、版、権限、送信データ、料金、実行先を実行前に確認する。
3. 本人が同意した仕事だけを端末、PC、Cloudへ届ける。
4. 実行中の状態を同じ仕事IDで追い、可能な範囲で停止・失効する。
5. 結果、失敗、使用版、費用を同じ履歴で確認する。

iPhoneやWeb clientはSkyの確認・指示画面になれるが、他社OSの制約を越えて任意の処理を常駐実行したり停止したりはできない。RockstarOS上のSkyは、端末内runner、永続job、署名Tool、更新・復旧をOSサービスとして管理する。Cloud/PCの外部処理について、相手が提供しない停止や費用上限を保証しない。

日中に開く理由は「完了した」「止まった」「確認が必要」「費用や入金に変化があった」。意味のない通知を増やさない。

### Wallet — お金と交換を確認する

利用可能、保留、未照合、費用、入金、ATM、ゲーム交換を取引種別で分ける。仕事の成功件数をお金へ換算しない。実際のお金は認証済み売上/入金と照合し、今の開発段階では合成データと明示する。

### Game — 任意の遊びの入口（提案）

接続済みゲーム、ゲームごとの資産、交換の見積/履歴、対応する起動先をまとめる。ゲームを入れなくてもOSは成立する。ゲームを消してもWalletや成立済みの交換記録を消さない。

最初は「接続・資産・交換」の軽い入口とする。ゲーム本編をOSへ固定搭載したり、現OSで未検証の3Dゲームを動くと表示したりしない。端末/PC/cloud等の実行先はゲームごとに確認する。

追加の達成演出を採用する場合、例として「成果が1件保存された」を小さなアニメーションで見せる。これは演出用の集計値であり、売上や換金できる通貨ではない。仕事本文や個人情報・残高はゲームへ勝手に渡さない。

ゲーム要素の狙いは、成果を理解しやすくし、望む人が遊びへ移れること。ログイン報酬で何度も開かせたり、金融操作をゲームの達成条件にしたりすることではない。

## 4. 仕組みの役割分担

| 部分 | 担当すること | 担当しないこと |
| --- | --- | --- |
| Rock OS | 本人操作、Sky/Wallet/Gameの表示、許可と重要通知、保存・更新・復旧 | 未確認の残高を真実として扱うこと |
| 共通Walletサービス | 所有者/端末の認証、残高・予約・取引記録、照合、同時支出の制御 | ゲームのスコアを根拠に現金を発行すること |
| ゲーム交換サービス | 見積、承認、両側の処理状況、再送の重複防止、照合 | ATMコードの流用、結果不明の自動返金 |
| 各ゲームのサーバー | そのゲームのアカウント、資産、付与/消費の確定結果 | Rock Walletの利用可能額を直接増やすこと |
| 作者向けAPI/SDK | 接続、sandbox、見積/承認の導線、状態/履歴照会、診断 | 秘密鍵をプレイヤーのクライアントへ埋め込むこと |
| ATM経路 | 独立した本人承認・払出し・結果照合、自社手数料0 | ゲーム交換の必須中継になること |

Walletを複数ゲームの共通窓口にすることと、全ゲームの通貨を同じ通貨へ統一することは別。Game AのコインとGame Bのコインは発行元・単位・交換条件を分ける。

### 共通Wallet化の先行条件（GX00）

現行backendはAliceという単一owner向けの公開fixture。複数端末試験も同じ契約の2台であり、一般の複数プレイヤー対応の証拠ではない。制限を外すだけで同じ台帳へ全員を流さない。

Wallet owner、購入端末、作者、game ID、game内player IDを分ける。本人認証・同意から接続を作り、両authority・owner/account・developer/game/player・権限・期限・失効を固定する。ゲーム作者やrequest本文の自己申告で他人のWalletを選べないようにする。既定ではゲームへWallet全残高・自動化収入・他ゲーム履歴を公開しない。

既存の1契約1DBを分離維持する方式か、版付き複数owner台帳へ移す方式かを、互換・認証・復旧・同時処理を比較する設計判断記録で選ぶ。OS単一ownerの最初の稼働試験は、この一般化を待たず進められる。

## 5. 交換でお金を壊さないルール

Wallet→ゲームは、条件提示→本人承認→利用可能額を予約→ゲーム側の付与→確定結果を照合→完了、という流れ。

途中で通信が切れたら「確認中」として保留を残す。ゲーム付与済みか不明なのに返金すると、通貨とお金を両方受け取れてしまうため、相手の記録を照会する。同じ操作を連打・再起動しても取引は1回だけ。

ゲーム→Walletは別の許可が必要な機能。方向を未確定のまま本番で有効にしない。ゲーム側の対象通貨の確定消費と、Wallet/承認済み清算先で確認できる交換原資が必要。作者が自由に増やせるポイント・bonus・スコアは、それだけでは換金できない。

ATM、月額、ゲーム交換が同時に同じ残高を使っても、全体で二重使用させない。Game Aの作者にGame Bや他プレイヤーの資金・履歴への権限を与えない。ATM障害はゲーム固有処理の前提にせず、ゲーム障害でATMを止めない。ただし共通Walletそのものが停止中なら、新しい資金確定はどちらも止める。

バックアップ復元では、同じ台帳を元と復元先の2か所で同時に支出可能にしない。外部で成立した取引は復元で巻き戻さず、再照合する。

## 6. ゲーム作者にとって使いやすい状態

作者が「ゲーム登録→テスト環境の設定→動くサンプル→プレイヤー接続/同意→合成交換→履歴/エラーの確認」まで進められることを最初の目標にする。

納品するのはAPI契約、reference SDK 1つ、合成通貨sandbox、最小サンプル、導入手順、診断/照会方法。最初から全ゲームエンジンの専用SDKを作らない。対象未指定なら汎用接続の小さな実装から始め、対応範囲を明記する。

2作者・2ゲーム・2owner/player（うち1ownerは2端末）で同じSDKを使い、認証有効の同一統合経路で正当な接続/交換と、別owner/gameへのアクセス拒否を試験する。同じplayer文字列や冪等キーでも混同せず、同ownerの月額は1回、別ownerは独立。同時処理/再起動/worker再利用で権限を漏らさず、一人の失効が他人へ波及しない。ゲーム利用だけで未同意のOS月額を開始しない。

新しい作業環境で手順を試し、設定数・初回交換までの時間・エラー原因特定時間を測る。外部の初見作者の評価は別pilotとし、内部テストで市場優位性を証明したことにしない。

## 7. OSとしてどこまで動けば最初の合格か

現状は旧基準版のQEMU起動実績と、native source回帰の成功がある段階。最新統合コードから作り直したOSの一連の合格証拠はまだない。

最初の合格は次のすべてを満たす「QEMU開発用OS雛形」。

1. 最新sourceと依存版を固定し、新しい領域からOS imageをbuildできる。
2. 実際にOSが起動し、native画面・Sky・模擬Walletを操作できる。
3. 既存商品を実処理し、許可外操作・通信・二重実行を拒否できる。
4. 正常終了・再起動で結果/同意/履歴が残る。
5. 更新失敗、起動無応答、容量不足、通信断を安全に処理し、復旧できる。
6. OSと補助サービスの保存範囲を明示し、新しい復元先で整合を確認できる。
7. 試験した版・環境・結果・ログ・未検証部分・再開手順が揃っている。

ゲーム機能を追加した候補は、ゲームが停止・過負荷になってもSky/Walletの基本操作を維持できることを追加確認する。LinuxにはCPU/メモリ等の[資源制御機構](https://docs.kernel.org/admin-guide/cgroup-v2.html)があるが、Rockで実際に設定・試験するまで達成済みにはしない。

公開試験鍵・合成データの開発検証なので、「本番で安全」「BlackBerryで動く」「実際のお金を交換できる」とはまだ言わない。型番/BSP/driver/電源・本番鍵/データ保護・実provider/運営条件は次の別ゲート。

## 8. 進める順番と今回やらないこと

承認後の順番は、main・native・設計reviewの統合→OS起動/安全基礎→既存商品とWalletの縦断操作→保存/復旧の受入。ゲームfixtureは共通Wallet基礎とGX00、動く作者SDKは交換契約に依存するが、実ゲーム・実資金の条件待ちからは独立して進める。段階ゲートは進捗JSONにも記録する。

最新A/B/data backup本体は再利用し、旧形式だけを見る復元試験入口を先に拡張する。profileごとの正本DB・追加表・外部backendを含め、旧/新backup形式を試験する。ゲーム台帳変更は旧台帳・旧client・直前OSとの互換を確認し、移行中断・未確定交換保持・A/B戻し・復元を試験する。現行の固定data ABIに移行engineはないため、安全に戻せると仮定しない。Game入口を採用しなくても台帳変更後のD4/D5再試験は必須。承認されたGame入口/演出を追加した場合も関連試験をやり直す。

ゲーム本編の新規開発、巨大ストア、全engine対応、新規課金モデル、実送金、ATM現金操作は今回の一括実行範囲外。後続の明示指示により、Developer Previewの配布準備と、対象1機種を固定した実機導入は実行範囲へ追加した。実機・ゲーム/providerが未指定でも進められる合成テストは分ける。

## 9. 承認済みの内容

- **中心と順番**：Sky＋Walletを中心に、まずQEMUで稼働/復旧できるOSを固める。
- **ゲーム要素**：ゲーム接続・交換機能を追加する。利用者によるGame入口の利用は任意。達成演出は見送る。
- **作者向け**：共通API/SDK・sandbox・サンプルから、組み込みやすさを測って改善する。
- **お金の境界**：ATMは独立・自社手数料0。ゲーム料金/交換方向/実資金は未確定のまま有効化しない。

設計v1.1の実装は承認済み。未決の実ゲーム/方向は、合成契約開発の承認を実交換の許可に置き換えない。

実行指示: `docs/prompts/os-operational-base-next.md`。不足の根拠: `docs/os-readiness-audit-20260909.md`。試験結果の記入用雛形: `docs/templates/os-acceptance-report.md`。

追加相談の [予測市場・ゲーム資産売買の検討メモ](market-exploration-20260909.md) は本設計の実装承認対象に自動追加しない。採用する場合は運営方式・対象地域・非換金試作の範囲を別途確認する。

## 10. 導入可能版とCM発表

CMに先立ち、まずMac/PCのfresh環境へ導入できるQEMU Developer Previewを作る。既存開発者VMや固定private pathへ依存させず、image/manifestの検証、作成、起動、通常終了、再開、backup、削除を一つの配布単位にする。

スマートフォン版は正確な1機種・variantを固定し、flashと純正復旧、driver、電源、署名OTAを実機で通してから導入可能と表示する。詳細な合格条件とCM表現は [導入・リリース計画](release-installation-plan-20260909.md) を正本とする。
