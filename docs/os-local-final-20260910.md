# Frozen 9abf78a local OS acceptance

**Status: required local D1, Hub D2, nonfinancial D3 and D4 scopes have passed; original whole-run failures remain retained.** This report covers the local Debian 13 ARM64/QEMU 10 environment and the immutable producer `9abf78a80d27aa9f847c4051d20e4c552e407276`. The [build/source record](os-final-build-20260910.md) retains the exact source archive, CI source-regression admission, original ARM64 source-regression failure, build environment and signed image/profile hashes. No observer correction described here rebuilds or replaces that candidate. D5/fresh installation, independent remote D6 and the native user flows have separate evidence and are not inferred from these scopes.

RQ06/09/10/12/13/16/17 | 明確な楽観主義 | 同じ配布物の起動・保存・障害回復を実測し、観測器の失敗を製品合格へ読み替えない | 既存 canonical verifiers と停止後 typed DB/source/image guards を再利用 | host-only 観測の限定修正と実行前 plan の追加 | 元の件数・期限・資源・OCR・全状態保持条件を維持 | 各原 report と追加実行の対応を以下に固定する。

## D4: complete 41-boot requirement coverage

The [continuation report](evidence/os-local-final/final-d4-continuation-report.json) completed at 11:01:01 UTC with `PASS_SCOPED`: 31 new boots and 10 complete original-case boots, explicitly totaling the required 41. Its SHA is `e93dca22501e92f31d098a99abfdb8f8731e77ca1ac8df1df94dc310be042f7d`; the [pre-execution plan](evidence/os-local-final/final-d4-continuation-plan.json) SHA is `5ccd33fc0e85df395f19a3996d58b9b928becf4c9eb289405cf7f6e143127892`.

| Case coverage | Original complete boots | New boots | Actual host observer |
| --- | ---: | ---: | --- |
| UI ready / absent / freeze | 2 / 4 / 4 | 0 | Frozen 9ab |
| UI crash | 0 | 4 | 9ab plus diagnostic `f7c391c` overlay |
| Signed A/B update | 0 | 8 | Frozen 9ab |
| Update faults | 0 | 13 | Frozen 9ab |
| Auth health | 0 | 3 | Frozen 9ab |
| Data ABI | 0 | 3 | Frozen 9ab |

The original whole run remains **FAIL**. It executed 13 boots, with 12 passing; crash boot 3 hit a diagnostic `/proc` reap race and crash boot 4 was not run. The two original successful crash boots are excluded from the 41-boot coverage, since the new complete crash case supplies all four. Thus 44 physical executions occurred across both runs; this is not a claim that either original whole run passed. The [D4 diagnosis](os-d4-probe-20260910.md) records the original serial evidence, fixed pidfd readiness condition, unchanged 180-second/12-GiB conditions, actual Linux 33-test proof and explicit four-file overlay.

All original 85 files and all completed-continuation 160 files were archived and independently read back off-VM, including every data/slot image, report, serial log and frame. The archives are respectively `c6009f5bbe563b08cd12ccbe23035a43503972e37a2908555de698789d83cbb3` (714,080,271 bytes) and `20ceb2e608a599076585f5d7d1c0abc87b239c810af7d8db822e052de3c21f26` (1,501,185,347 bytes), under `/Users/kaiya/Documents/Codex/2026-09-10/j-a-r-v-i-s/work/os-acceptance/`. Only verified large Linux copies were retired after owned QEMU exit; source and frozen production images, small userdata and all small raw logs/reports/frames remain. Archive inventories and readback/retirement receipts are retained beside those archives.

## D2: exact lifecycle scope

The unchanged canonical lifecycle verifier completed two normal boots, 16 operations and five actual Hub jobs on the same Game-profile image. Installation 1.0 → update 1.1 → rollback, disable → reconsent, deletion with saved-result history, and reinstall all passed. The [original report](evidence/os-local-final/final-d2-lifecycle-report.json) SHA is `1cea41d7c8a19ba6ebdf636172d424fbc3789f152ce119076a600e092ca3d077`; [raw plan](evidence/os-local-final/final-d2-lifecycle-plan.json) SHA is `2388291c5fa66a109ac1a649b69e1607c1e9aaa1dfcf823420d2d82870687373` and the canonical plan hash is `dc7a4a39e375721418ec19e5d118e8f458e46ab12ce6c1ccfb31d22b4e47ad7a`. All stopped-guest retention checks, clean filesystems and complete stopped external authority snapshots matched.

Its scope is **PASS_SCOPED for Hub**, with the report's whole-D2 status and financial `NOT_RUN` fields preserved. Native Wallet/Game first-use and financial scenarios must supply separate same-candidate evidence. An intermediate human progress update incorrectly interpreted a mutable screenshot as a stalled reconsent; immutable timestamped input/result frames and the final unchanged verifier showed normal progression. No extra input, source change, retry or deadline extension occurred. The [observation correction](evidence/os-local-final/final-d2-observation-correction.json) retains the exact frame hashes and the final result. This was not a failed D2 execution.

## D1/D3: original completed cases and Hub preflight correction

The [original wrapper report](evidence/os-local-final/d1-d3-original-report.json) remains **FAIL**, as does its [nonfinancial child report](evidence/os-local-final/nonfinancial-original-report.json). Before the failure, D1 normal boot/restart supplied two boots, system/power supplied two, D3 guest isolation supplied one with 47 isolation and 45 SDK/tool checks, Store supplied two, remote execution supplied two, and registry rejection supplied one. Those six complete gates total ten boots, all with exact report hashes and complete stopped authority state retention. Financial guest assertions remain explicitly `NOT_RUN` in the Game isolation scope.

Hub faults failed before any guest boot: `debugfs rdump` within a namespace mapping only the invoking user could not chown the profile's private UID/GID 1003 files. [Original error](evidence/os-local-final/hub-original-fail.json) and [raw log](evidence/os-local-final/hub-original-fail.log) are retained. The actual Wallet configuration/token remained mode 0600 owned by UID/GID 1003; there was no image defect or guest-state mutation.

Diagnostic commit `7e9dcc9e4dafb686f344a790482892c489699757` changes only the host Hub verifier and its host evidence tests. It delegates manifest collection to the frozen candidate's existing `image_inventory.py`: inode-number read-only flat exports avoid chown and preserve every selected path's kind/mode/source UID/GID and content. Symlink target strings are now represented by exact byte count and SHA256, explicitly recorded as manifest schema 2. The old five runtime prefixes, 20,000-path limit, fixed byte limit, complete existing-entry equality and exactly three new fixture paths remain required. All separately read runtime sources and signed packages are checked against their inventory path, size and content hash before use. The helper's hash is included in the Game gate's before/after source pin set. Guest fixture behavior, signatures, budgets and all three crash/deadline/recovery assertions are unchanged.

[Linux host tests](evidence/os-local-final/inventory-tests.log) and [unchanged inventory helper tests](evidence/os-local-final/image-inventory-tests.log) passed all 24 cases. The [actual same-image preflight](evidence/os-local-final/inventory-preflight.json) read 1,945 runtime paths, retained both private files' ownership/mode and checked every old entry unchanged with only the three declared additions in the disposable fixture. Both host observer modules are absent from production runtime. Exact 1,364 source files and the original triple were rechecked unchanged; the separate probe checkout differs only in the two explicitly hashed files. This preflight did not boot a guest.

The [new plan](evidence/os-local-final/final-hub-continuation-plan.json) was fixed before execution, SHA `2ca063992194547ed84b3751e2f6062bac148de1f01a7ca46e0b1207318c1563`. It requires all three Hub fault boots with the original 240-second per-boot and 3-second Hub deadlines, full runtime/receipt/replay/closed-DB evidence and exactly unchanged external authority state. It maps ten completed original boots plus these three to 13 required D1/D3 boots while keeping every original whole-run failure. The [new report](evidence/os-local-final/final-hub-continuation-report.json) completed with `PASS_SCOPED`, SHA `9fa7878e3b46bf1a5e3aa77fdf89a0fcd18a5ce36afee665d7cee063dcd7b899`: crash 53.183 seconds, deadline 48.298 seconds and recovery 32.623 seconds. All stopped guest and external authority state checks passed. This supplies the three missing complete cases, yielding 13 required D1/D3 boots with the ten unchanged completed original boots. It does not change either original whole-run FAIL to PASS. The [continuation driver](evidence/os-local-final/run-final-hub-continuation.py) records and checks this explicit correspondence.

## Fresh native UI handoff

The read-only [final collector](evidence/os-local-final/collect-final-local-acceptance-v2.py) independently rechecked all 1,364 producer source files, the frozen triple/profile, the D4 and D2 records, every completed D1/D3 leaf report hash, and the retained original failure hashes. Its [handoff report](evidence/os-local-final/final-local-os-handoff-report.json), SHA `b6bbbbe65ea51bba3e951f21d6895d605f44ddbd15535608cb34e63c1dd0a9ae`, reports `PASS_SCOPED_LOCAL_D1_D2_HUB_D3_NONFINANCIAL_D4`.

At the recorded handoff time, the same default authority was stopped and its complete typed databases and protected identity JSON matched the original empty prepared snapshot exactly; all 19 required financial/authentication/membership tables were empty. No QEMU was running, ports 9443/9444/9641–9643 were free, and the `rock-final-c-ui` device directory did not exist. The root C configuration was `/var/tmp/rock-release-os-20260910/final-9abf78a-empty-authority/root-c-device.json`, SHA `1fbd4924c545823b313a6852b428aadce3f9ef2bc1dc11d57b2d078fb7316b8f`. The integration lead received this fresh state to begin the native Game/Wallet/remote-product/demo sequence. These are observations at handoff, not claims that the authority remains empty after those authorized user operations.

The independent remote D6 completed successfully on GitHub run `34465689434` using the same frozen candidate and a separate fresh authority, as allowed by the pre-execution host-location amendment. D's independently audited raw results and B's fresh install/aggregate restore remain separate records. The subsequently authorized [one exact-source native repeat](os-native-repeat-20260910.md) passed all 14 checks and 1,631 Python executions after C UI/encoding and other heavy work ended. The original ARM64 FAIL, the exact original CI freeze binding, and separate later integration-branch CI failures remain unchanged; no cause is inferred from the repeat.

The [client/slot/ledger compatibility matrix](os-final-compatibility-20260910.md) records ten explicit expected/actual scopes with raw per-test and per-SIGKILL hashes. Same-candidate derived-slot recovery and foreign-ABI refusal are not an old b828 reader/writer test; automatic existing-local-to-Game migration and general old-OS rollback remain unimplemented or untested.
