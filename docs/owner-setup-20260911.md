# kaiya の決定と残る設定 — 2026-09-11

権利者の公開名は **kaiya**、自作部分の改変・再配布を許可する意向を受領した。具体的なライセンス条文は未指定なので MIT を草案とし、第三者由来の条件と rc2 の NOT_CLEARED を保持する。CMは今回の回答を優先して **制作途中** と訂正し、69秒候補を完成品として掲載しない。

別の署名承認者は未指定（回答を「いない」と解釈）。二人承認は現行GitHub経路の設計であり、利用者の原文の必須条件ではない。既存保護設定は保持し、本人が対象候補を明示承認して隔離環境で署名する別経路を準備する。本番鍵の生成・登録・署名は未実施。

## 新しいSites

利用者の「作成する」に基づき、元サイトのNOT_FOUNDを理由に自動で置換せず、明示された新規作成として別サイトを登録した。旧project設定は `data/sites-transition-20260911.json` に保持し、元サイト・元DBを変更しない。既存のHub、PWA、認証付き実行管理、手入力会計、両DB履歴を統合したコードを再利用する。新しいD1は空から開始し、旧利用者データの復元完了とは表示しない。

対象は本人限定のサイト確認。一般公開、製品main merge、課金、実資金、実機書込の実行許可ではない。CMは未選定のため非表示とし、公開前の候補案内・既存の技術デモ・導入手順を使う。

## 実施した確認

Webの `npm run verify` に合格（型・lint・93 tests・build・API 143 assertions）。新規D1と両方の旧DB履歴からの移行試験、認証境界・利用者分離・保存/再起動のAPI確認を含む。`npm run os:check`、production依存監査、`git diff --check` も成功。ローカル導入ページはHTTP 200、ブラウザで案内・技術動画・導入リンク・kaiya表記を確認した。サイト本番上の同じ確認やOSのキャンセル/メモリ受入を合格に置き換えない。

Sitesへの保存・本人限定公開は、この変更を保存した後に実施し、その実応答を後続の公開記録へ残す。
