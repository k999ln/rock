# Exact 9ab native source repeat — 2026-09-10

The one authorized repeat of immutable producer `9abf78a80d27aa9f847c4051d20e4c552e407276` **passed all 14 checks and 1,631 Python test executions, without skips**. The unchanged canonical `scripts/test-native.py` ran at 12:59:18.903567–13:02:33.547305 UTC (194.644 seconds); its main 1,367-test suite completed in 151.789 seconds. This is host source-regression evidence, separate from OS boot, actual device, provider and financial acceptance.

RQ09/12/16/17 | 明確な楽観主義 | 元ARM64回帰の10 TLS失敗が未解決 | 同じGit archiveとcanonical全回帰を再利用 | C利用一周・録画・encode・build・重いbrowser QAの停止後に新outputで一度だけ実行 | 元14/1631・全期限・skip禁止を維持 | 全log/SHA・環境・元失敗と新結果を独立保存。

The integration lead explicitly confirmed that C UI, recording/encoding, build and heavy browser QA had ended. A [pre-execution plan](evidence/os-native-repeat-20260910/plan.json) fixed the source, original failure hashes, command and required counts before the run. Its SHA is `4654bbf7f6b2e407d9f42eb00169723902e28ef999fff3d8c2b0256fcc7d6f0d`. The [external observer](evidence/os-native-repeat-20260910/run-final-native-r2.py) used `umask 0022`, the required system PATH, and refused an existing output directory, active QEMU/unittest process or occupied fixture port. It did not change runtime, tests, timeouts or the freeze's source-test binding. Linux was Debian 13 ARM64, Python 3.13.5, kernel 6.12.95; the Mac was on AC power at 100% charge. Before/after environment records avoid process arguments and secrets.

The [unmodified canonical report](evidence/os-native-repeat-20260910/native-report.json) SHA is `69b519c01696dcd990938fb77187f290de3aab07649798a3dacd60873f23b110`. Its 694 input hashes exactly equal the original ARM64 run, all 14 raw log hashes verified on Linux and after host copy, and the source was unchanged with no changed inputs. The [repeat summary](evidence/os-native-repeat-20260910/repeat-report.json), SHA `f0e2f7890f289da5d1625c6aa67a0d1c857df99cfe49ab2af8906f4d7bd4e8c7`, separately rechecked all 1,364 producer files and the frozen image triple. No QEMU or unittest process remained after completion. All 23 host evidence files are recorded in the [readback inventory](evidence/os-native-repeat-20260910/host-readback.json); full raw logs remain at `/Users/kaiya/Documents/Codex/2026-09-10/j-a-r-v-i-s/work/os-acceptance/native-r2-recovered/`.

| Source / execution | Observed outcome | Interpretation |
| --- | --- | --- |
| 9ab original ARM64 | FAIL: 14 checks / 1,631 executions, ten TLS read/frame errors | Original report and errors remain unchanged; cause undetermined. |
| 9ab original CI `34459916162` | PASS: 14 / 1,631 on x86_64 | This original report remains the immutable freeze's source-test admission. |
| 9ab one clean repeat | PASS: 14 / 1,631 on ARM64 | None of the original ten errors reproduced in this run. This does not identify or fix their cause. |
| e430 CI `34477407336` | FAIL: 14 / 1,649, one initial approval TLS read timeout | Separate later source/run; retained independently. |
| ba900 CI `34478360425` | FAIL: main suite reached its unchanged 600-second timeout, exit 124; remaining 13 checks NOT_RUN | Separate later source/run; no complete main-suite count was reported. Do not interpret its raw `tests: 0` summary as zero tests executed. |

The [e430 failure record](evidence/os-native-repeat-20260910/independent-e430-ci-failure.json) binds the complete traceback to initial `approve` in `test_author_revocation_survives_full_backend_restart_without_disabling_other_game`, through TLS receive to `BackendUnavailable`. Its observed test and two transport source files are byte-identical to 9ab; this narrow comparison does not establish a cause. The [ba900 timeout record](evidence/os-native-repeat-20260910/independent-ba900-ci-timeout.json) preserves the main log ending at `test_adopted_original_rows_then_real_three_device_four_game_connections` without an outcome. Both named cases completed `ok` in the 9ab repeat; that does not turn either later CI run into a pass or prove why it failed.

No CI retry, deadline extension, test removal, source change or additional native repeat was performed by A. The later integration-branch CI failures remain open evidence for the integration lead's separate diagnosis. They are not replaced by this same-producer success, and the original failures are not attributed to ARM64, CPU contention or a fixed runtime defect without evidence. The [local OS acceptance scope](os-local-final-20260910.md) and [compatibility matrix](os-final-compatibility-20260910.md) remain independently bounded records.
